const teamMembers = document.querySelectorAll('.memberSocials');
const memberSocials =  document.getElementsByClassName("memberSocials");

console.log(teamMembers);

for (element in teamMembers){
  console.log(element);
}    
